The airfare cost exceeds the amount allowed for this trip. Increase the trip fee to at least {{ (config('travelpolicy.minimum_trip_fee_cost_ratio') * 100) }}% of the per-person cost for this trip. The per-person cost is ${{ number_format($totalCost) }}, so the minimum trip fee for this itinerary is ${{ number_format(ceil($totalCost * config('travelpolicy.minimum_trip_fee_cost_ratio'))) }}.
